import { atom } from 'jotai';

export const idProductSelected = atom(1);

export const atomCategories = atom([]);
export const atomExtras = atom([]);
export const atomIdCategorySelected = atom(1);
export const atomCategorySelected = atom({});
export const productsListSelected = atom([]);
export const atomProductSelected = atom({});
